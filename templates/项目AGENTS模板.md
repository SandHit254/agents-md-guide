# 项目 AGENTS.md 模板

> **体量红线 < 30 KB。每条信息只回答"去哪找"，不回答"是什么"。**
> 使用前删掉本引用块与所有 `<占位符>`。详细规范见 [README](../README.md)。

# <项目名> 项目 AGENTS.md

<一句话说清这个项目是什么、给谁用。>

**全局工作规则见全局 AGENTS.md**（本文件只记本项目特有的事实）

---

## 一、项目基线（一眼看清当前状态）

| 项 | 值 |
|----|-----|
| 仓库 | `<path>` / `<git remote>` |
| 当前版本 | `<版本号>` |
| 线上环境 | `<地址>` |
| 技术栈 | `<语言 + 框架 + 数据库>` |
| 部署方式 | `<一句话>` |

---

## 二、模块状态（**状态 + 一句话 + 指针**）

> 不要在表格里写历史、遗留问题、验证细节 —— 那些进对应模块的落地文档。

| 模块 | 状态 | 详见 |
|------|------|------|
| `<模块A>` | 已完成 / 进行中 / 未开始 | `记录/应用/<A>/` |
| `<模块B>` | 已完成 | `记录/应用/<B>/` |

---

## 三、关键决策（**为什么这么做**）

> 只记"为什么不选另一条路"，这是最容易失传的信息。
> 每条不超过 3 行，超过就下沉到 `记录/方案评估/`。

| 决策 | 选择了 | 放弃了 | 原因 |
|------|--------|--------|------|
| `<决策点>` | `<方案A>` | `<方案B>` | `<一句话>` |

---

## 四、环境（硬事实）

| 环境 | 地址 | 管控 |
|------|------|------|
| DEV | `<地址>` | 日常可操作 |
| STAGING | `<地址>` | **非只读操作需批准** |
| PROD | `<地址>` | **非只读操作需批准，标准更严** |

**凭据**：见 `<记忆名 或 凭证管理位置>`，**不在本文件明文写**。

---

## 五、目录结构（只列一层，说明"什么进哪"）

```
<项目根>/
├── 记录/            ← 一切实质产出（按应用/主题分类）
│   ├── 应用/<模块>/   品牌定制 / 功能开发
│   ├── 经验/         故障排查复盘
│   ├── 方案评估/     动手前的决策论证
│   └── 文档规范.md   写作规范（四段式 + 维护评估七维）
├── 交付物/          ← 一切产出物（包/镜像/二进制），单一登记处 交付物/台账.md
└── <源码目录>/
```

**暂存区**：临时产物落 `<项目外暂存区路径>`，按 6 类分层，见该目录 `README.md`。

---

## 六、坑速查（**现象 + 指针，禁止写完整描述**）

> **反模式**：单元格里塞 300 字把根因、过程、命令全写完 → 该下沉。
> **正确**：只写"看到什么现象"，详细过程进 `记录/经验/<坑名>.md`。

| 现象 | 详见 |
|------|------|
| `<一句话描述症状>` | `记录/经验/<坑名>.md` |
| `<一句话描述症状>` | `记录/经验/<坑名>.md` |

---

## 七、常用命令

```bash
# 构建
<命令>
# 测试
<命令>
# 部署
<命令>
# 断链检查（文档指针是否都跳得通）
<命令>
```

---

## 八、变更记录（**日期 + 一句话 + 指针，禁止写明细**）

| 日期 | 变更 | 详见 |
|------|------|------|
| `<YYYY-MM-DD>` | `<一句话>` | `交付物/台账/<应用>.md` |

---

## 自查（提交前跑一遍）

- [ ] 全文 < 30 KB
- [ ] **没有任何单元格超过 2 行**
- [ ] 没有单行超过 300 字
- [ ] 每个 `详见` 指针都真实存在（跑断链检查）
- [ ] 没有把"内容"写进来 —— 每节都只回答"去哪找"
- [ ] 版本号/环境地址/状态是**当前**的，不是上次改时的
